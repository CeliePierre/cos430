const express = require('express');
const router = express.Router();

// TODO when we set up the Authentication
// router.post('/register', registerUser);
// router.post('/login', loginUser);

module.exports = router;
